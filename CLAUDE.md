# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

`forge` is a brand-new, empty repository. As of now it contains only a `README.md` placeholder — no source code, build system, package manifest, tests, or CI configuration exist yet.

There are no build/lint/test commands to document and no architecture to describe because no code has been written. When the first real code is added to this repo, update this file to cover:

- Build, lint, and test commands (including how to run a single test)
- The overall architecture and how major components fit together
- Any project-specific conventions

Until then, treat this file as a placeholder and avoid inventing commands, structure, or conventions that don't exist in the repo.
